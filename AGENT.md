# 用V语言编写web版应用，全面展示我们这个世界的方方面面数据。

## 技术栈：
- 编程语言： V语言，vlang.io，全面负责所有业务逻辑，涉及内置库os、time、veb、json2、net.http、db.mysql等
- 数据库：mysql 8.0+
- 前段：html及V语言模板，必要的js、css

## 附件：
- worldbank数据展示示例：./worldbank
- IMF数据展示示例：./IMF_shows
- world_market数据展示示例：./world_market

## 要求：
- 把附件所示程序（均可单独运行）集成到一个web应用中，页面右侧显示边栏，包括内容列表和分类搜索
- 由于数据取得比较费时，程序启动时显示数据库已有数据，单独进程后台获取数据，存入数据库，数据取得完成后自动更新页面
- 程序具有自动更新自身的功能
- 附件内数据库一般为sqlite3，请导入mysql，作为初始数据
- 可补充数据源、国家/地区、指标
- 要编写、更新文档
- 优先在wsl调试程序
- 最终程序放在./world_app目录里
- html用V语言模板，单独存放
- 调试完成后，不在依赖所附程序
- 程序启动9秒后，在东打开浏览器
- 必须防止sql注入、url注入
- log记录
-
